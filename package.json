{
  "name": "json2csvconvert-cli-tool",
  "version": "1.0.0",
  "description": "usage: jsonconvert --read=file.json --write=file.csv",
  "main": "bin/jsonconvert.js",
  "preferGlobal": true,
  "bin": { 
    "jsonconvert": "bin/jsonconvert.js" 
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Offner",
  "license": "UNLICENSED",
  "dependencies": {
    "yargs": "^15.4.1"
  }
}
